syntax = "proto3";

package proto.ext.daemon;

message UrlResponse {};

message UrlRequest {
  string url = 1;
};

message Request {
  oneof payload {
    UrlRequest url = 1;
  };
};

message Response {
  oneof payload {
    UrlResponse url = 1;
  };
};
