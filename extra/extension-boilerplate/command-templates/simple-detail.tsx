import React from 'react';
import { Detail } from '@vicinae/api';

export default function SimpleDetail() {
	return (
		<Detail markdown="# Hello world\nYour extension is successfully running. Time to get to it!" />
	);
}
